{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="min-h-screen bg-background flex flex-col">
        <nav class="bg-white shadow-lg">
            <div class="max-w-6xl mx-auto px-4">
                <div class="flex justify-between items-center py-4">
                    <div>
                        <a href="#" class="text-lg font-semibold text-gray-500">ChatApp</a>
                    </div>
                    <div class="flex items-center space-x-4">
                        <a href="#" class="text-gray-700 hover:text-gray-900">Accueil</a>
                        <a href="#" class="text-gray-700 hover:text-gray-900">À propos</a>
                        <a href="#" class="text-gray-700 hover:text-gray-900">Contact</a>
                        <div class="relative">
                            <button id="userMenuButton" class="flex items-center text-gray-700 hover:text-gray-900 focus:outline-none">
                                <span class="mr-1">👤</span>
                                <span id="accountStatus">Compte</span>
                            </button>
                            <div id="userMenu" class="absolute right-0 mt-2 w-48 bg-white rounded-md overflow-hidden shadow-xl z-10 hidden">
                                <a href="#" id="loginLink" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Se connecter</a>
                                <a href="#" id="registerLink" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Créer un compte</a>
                                <a href="#" id="logoutLink" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 hidden">Se déconnecter</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </nav>

        <div class="flex-grow flex items-center justify-center">
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full">
                <h1 class="text-2xl font-bold text-primary mb-8">Bienvenue sur ChatApp</h1>

                <div id="userButtons" class="space-y-4 hidden">
                    <button id="createChatroomButton" class="w-full bg-primary text-white py-3 px-4 rounded hover:bg-tertiary transition duration-300">
                        Créer une Chatroom
                    </button>
                    <button id="myChatroomsButton" class="w-full bg-secondary text-white py-3 px-4 rounded hover:bg-tertiary transition duration-300">
                        Mes Chatrooms
                    </button>
                </div>

                <div id="guestMessage" class="text-center text-gray-600">
                    <p>Connectez-vous pour créer ou accéder à vos chatrooms.</p>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Le script JavaScript reste inchangé
    </script>
{% endblock %}